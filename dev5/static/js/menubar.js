var setCredentials = function() {
    /* $.ajax({
        'url' : 'credentials/ajax_set',
        'dataType' : 'json',
        'data' : {'objuuid' : objuuid},
        'success' : function(resp) {
            inventoryObject = resp;
            editRFC();
        }
    }); */
}
